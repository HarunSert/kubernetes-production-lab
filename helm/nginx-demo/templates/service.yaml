apiVersion: v1
kind: Service
metadata:
  name: nginx-demo-service
spec:
  type: {{ .Values.service.type }}

  selector:
    app: nginx-demo

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
